export default defineAppConfig({
  ui: {
    // Semantic color seeds from Social House (see tokens/color.tokens.json):
    // Nuxt UI aliases primary/neutral onto the OKLCH palette owned by tokens/.
    colors: {
      primary: 'indigo',
      neutral: 'slate',
    },
  },
})
